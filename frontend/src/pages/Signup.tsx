import SignupForm from '@/components/SignupForm';

/**
 * Signup page - wrapper for SignupForm component
 * SEO: Create account page for IrriPredict irrigation prediction system
 */
const Signup = () => {
  return <SignupForm />;
};

export default Signup;
