import LoginForm from '@/components/LoginForm';

/**
 * Login page - wrapper for LoginForm component
 * SEO: Login page for IrriPredict irrigation prediction system
 */
const Login = () => {
  return <LoginForm />;
};

export default Login;
